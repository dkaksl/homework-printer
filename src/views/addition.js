import { Component } from "react";
import { Printable } from "../components/printable";

export class Addition extends Component {
  render() {
    return (
      <Printable>
        <div>This is where the homework content should go</div>
      </Printable>
    );
  }
}
